# Command: /resume-templates-sync
Synchronize the resume template registry and regenerate supporting metadata.

Steps:
1. Fetch the latest template definitions and verify schema compatibility.
2. Update preview assets, tokens, and component mappings.
3. Refresh the template index and confirm availability in the builder UI.
