# Week 06: React Mini Apps

This directory contains a collection of small React applications built to demonstrate core React concepts, state management, and API integration.

## 🚀 Projects

### 1. Memory Game
A classic card matching game.
*   **Features:** Card flipping logic, match verification, move counter, and reset functionality.
*   **Key Concepts:** `useState`, `useEffect`, conditional rendering, array shuffling.

### 2. Image Search
A search interface for finding images via an external API.
*   **Features:** Search bar, grid layout for results, loading states.
*   **Key Concepts:** API fetching (`fetch` or `axios`), handling asynchronous data, form handling.

## 🛠 Tech Stack

*   **Core:** React 19
*   **Build Tool:** Vite
*   **Routing:** React Router DOM v7
*   **Styling:** CSS Modules / Standard CSS

## 📦 Setup Instructions

1.  **Navigate to the project directory:**
    ```bash
    cd react_mini_apps
    ```

2.  **Install dependencies:**
    ```bash
    npm install
    ```

3.  **Start the development server:**
    ```bash
    npm run dev
    ```

4.  **Open the application:**
    Navigate to `http://localhost:5173` to view the app hub.